# lib-unused — archived factory modules

Historical Swissystem7 factory drafts with self-tests. **Not loaded by any live page.** Product logic lives in `src/lib/` (and `availability-model.js` for the map demo).

Kept only as history. Do not re-wire without a real caller and a test under `npm test`.
